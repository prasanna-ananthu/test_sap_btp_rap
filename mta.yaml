_schema-version: "3.2"
ID: travustaplptravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travustaplptravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travustaplptravelproc-destination-service
    parameters:
      content-target: true
  - name: travustaplptravelproc-repo-host
    parameters:
      service-key:
        name: travustaplptravelproc-repo-host-key
  - name: travustaplptravelproc-uaa
    parameters:
      service-key:
        name: travustaplptravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travustaplptravelproc_html_repo_host
          ServiceInstanceName: travustaplptravelproc-html5-srv
          ServiceKeyName: travustaplptravelproc-repo-host-key
          sap.cloud.service: travustaplptravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travustaplptravelproc_uaa
          ServiceInstanceName: travustaplptravelproc-xsuaa-srv
          ServiceKeyName: travustaplptravelproc-uaa-key
          sap.cloud.service: travustaplptravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travustaplptravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travustaplptravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travustaplptravelproc.zip
      name: travustaplptravelproc
      target-path: resources/
- name: travustaplptravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travustaplptravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travustaplptravelproc-destination-service
    service-plan: lite
- name: travustaplptravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travustaplptravelproc-xsuaa-srv
    service-plan: application
- name: travustaplptravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travustaplptravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
